/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { NodosPipe } from './nodos.pipe';

describe('Pipe: Nodose', () => {
  it('create an instance', () => {
    let pipe = new NodosPipe();
    expect(pipe).toBeTruthy();
  });
});
